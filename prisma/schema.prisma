// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
}

model BillItem {
  id       Int      @id @default(autoincrement())
  batchId  String
  name     String
  type     String
  price    Int
  quantity Int
  expiry   DateTime
  amount   Int
  billId   Int
  Billing  Billing  @relation(fields: [billId], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model Billing {
  id           Int        @id @default(autoincrement())
  BillItem     BillItem[]
  DayBillingId Int
  DayBilling   DayBilling @relation(fields: [DayBillingId], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model DayBilling {
  id      Int       @id @default(autoincrement())
  date    DateTime  @unique
  Billing Billing[]
  userId  String
  User    User      @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([date, userId])
}

model Items {
  id     Int    @id @default(autoincrement())
  name   String
  type   String
  price  Int
  userId String
  User   User   @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([name, userId])
}

model User {
  id          String       @id
  name        String       @default("Unnamed User")
  Item        Items[]
  DayBillings DayBilling[]
}
